# BOONWAVE v6.0.58 — UNIFIED CARD ACTIONS

Исправлено:

- единый механизм одиночного и двойного касания для всех типов карточек;
- карточка «Человек» открывается тем же `openDetail(node)`, что проект, процесс, идея и цель;
- при одиночном касании DOM карточек больше не пересоздаётся;
- панель из трёх кнопок не выезжает из-под карточки, а надёжно и мягко появляется ниже неё;
- панель унифицирована для всех сущностей;
- устранён второй внешний контур выбранной карточки;
- увеличено окно двойного касания до 430 мс для iOS;
- обновлены уникальные имена JS/CSS и service worker до 6.0.58.

Подтверждено статически: синтаксис JS, ссылки index.html, версии service worker, наличие единого обработчика. Поведение касаний на реальном iPhone требует проверки.
