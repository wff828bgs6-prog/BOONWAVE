# BOONWAVE v6.0.57 — CACHE & GESTURE RECOVERY

Ключевые изменения:

- JavaScript и CSS получили уникальные имена версии: `app.v6.0.57.js`, `styles.v6.0.57.css`. Старые файлы в репозитории больше не могут быть подхвачены текущим `index.html`.
- Service Worker переведён на cache namespace `boonwave-6.0.57`, удаляет старые кэши BOONWAVE и использует network-first для HTML/JS/CSS.
- Исправлена версия service worker: в v6.0.56 внутри ошибочно оставалась `6.0.54`.
- Добавлен запрет canvas double-tap/fitAll после взаимодействия со связью.
- Усилен финальный CSS-контракт выезжающей панели действий.

Проверять только по каноническому URL и после полного обновления страницы/PWA.
